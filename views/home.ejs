

<%- include("partials/header"); -%>
    <h1>Home</h1>
    <p> <%= homeStartingContent %></p>
    <% postsMade.forEach(function(post){ %>
        <h1><%= post.title %></h1>
        <p><%= post.body.slice(0,140) %> <a href="posts/<%= post._id %>">...Read More</a></p>
   <% }) %>
<%- include("partials/footer"); -%>
